# /etc/maludb/email-hook-relay.env -- where the node's Auth email hook relay sends.
# The control plane's INTERNAL listener. An address only; nothing here is secret.
MALUDB_EMAIL_HOOK_UPSTREAM=10.0.0.10:8111
