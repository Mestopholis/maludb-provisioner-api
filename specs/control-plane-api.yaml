# Generated by scripts/export-openapi.py -- do not edit by hand.
#
# ADR-024: the FastAPI application is authoritative for this contract. CI fails
# if this file drifts from the app. To change the API, change the routes and
# regenerate.
#
# NO AUTHENTICATION IS DECLARED OR ENFORCED YET. These routes are unauthenticated
# and the service refuses to start with MALUDB_ENV=production for that reason.
# Platform-user authentication -- sessions and personal access tokens, described
# in docs/ACCOUNTS.md -- lands in Phase 01 slice 2 and will appear here once the
# application actually enforces it.
components:
  schemas:
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          title: Detail
          type: array
      title: HTTPValidationError
      type: object
    Health:
      properties:
        status:
          title: Status
          type: string
      required:
      - status
      title: Health
      type: object
    PlanOut:
      properties:
        code:
          title: Code
          type: string
        limits:
          additionalProperties: true
          title: Limits
          type: object
        name:
          title: Name
          type: string
      required:
      - code
      - name
      - limits
      title: PlanOut
      type: object
    ProjectOut:
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        display_name:
          title: Display Name
          type: string
        project_ref:
          title: Project Ref
          type: string
        status:
          title: Status
          type: string
      required:
      - project_ref
      - display_name
      - status
      - created_at
      title: ProjectOut
      type: object
    ValidationError:
      properties:
        ctx:
          title: Context
          type: object
        input:
          title: Input
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          title: Location
          type: array
        msg:
          title: Message
          type: string
        type:
          title: Error Type
          type: string
      required:
      - loc
      - msg
      - type
      title: ValidationError
      type: object
info:
  description: Platform-user credentials only. Unrelated to project API keys or to end-user tokens issued
    by a tenant's Auth service.
  title: MaluDB Control Plane API
  version: 0.1.0
openapi: 3.1.0
paths:
  /healthz:
    get:
      operationId: healthz_healthz_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
          description: Successful Response
      summary: Liveness probe
      tags:
      - health
  /readyz:
    get:
      operationId: readyz_readyz_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
          description: Successful Response
      summary: Readiness probe
      tags:
      - health
  /v1/organizations/{org_id}/projects:
    get:
      operationId: list_projects_v1_organizations__org_id__projects_get
      parameters:
      - in: path
        name: org_id
        required: true
        schema:
          format: uuid
          title: Org Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ProjectOut'
                title: Response List Projects V1 Organizations  Org Id  Projects Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: List an organization's projects
      tags:
      - projects
  /v1/plans:
    get:
      operationId: list_plans_v1_plans_get
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/PlanOut'
                title: Response List Plans V1 Plans Get
                type: array
          description: Successful Response
      summary: List active plans and their limits
      tags:
      - plans
  /v1/projects/{project_ref}:
    get:
      operationId: get_project_v1_projects__project_ref__get
      parameters:
      - in: path
        name: project_ref
        required: true
        schema:
          title: Project Ref
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectOut'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Get a project by reference
      tags:
      - projects
