# MaluDB Platform — Claude Code Adapter

@AGENTS.md

Use `AGENTS.md` as the canonical repository-level agent instructions.

For significant tasks, use the same `docs/`, `specs/`, `tasks/`, and `plans/` workflow described there. Do not create a competing Claude-specific architecture or planning hierarchy.
